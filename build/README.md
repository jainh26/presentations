# NikoMom — Workshop 1 (Reimagined)

Board-level strategic recommendation deck generated by the Global Brand Council
brief, on-brand with the Panacea Biotec / 7NShop presentation spec.

- `brandkit.py` — reusable Panacea Biotec pptx helpers (palette, layout, components)
- `build_deck.py` — builds `../NikoMom_Workshop1_Reimagined.pptx` (36 slides)

Rebuild: `python3 build_deck.py` (requires `python-pptx`).
